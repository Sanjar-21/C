#include <math.h>
#include <stdio.h>

int main(void) {
  double x = 2.4;
  double y = 3.1;
  double resolt = pow(x, y);
  int a = 3;
  int b = 3;
  int num = pow(a, b);
  printf("num: %d\n", num);
  printf("resolt: %f\n", resolt);
  return 0;
}
