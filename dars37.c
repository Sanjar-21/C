#include <stdio.h>

#define LEN 10
// c da comintlar 2 hil buladi bir qatorni olish va kup qatorli
int main(void) {
  // bir ga mislol int arr[] = {10, 10, 10, 10, 10, 10, 10, 10, 10, 10};

  /*int num;

  for (int i = 0; i < LEN; i++) {
    num += arr[i];
  }

  printf("arrayni urtasi arr = %d\n", num / LEN);
  return 0;*/
}
